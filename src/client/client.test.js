test("test btn search", async () => {
  const btn = document.getElementById("search-btn")
  expect(btn).toBeDefined()
})

test("test input location", async () => {
  const btn = document.getElementById("location")
  expect(btn).toBeDefined()
})

test("test input date", async () => {
  const btn = document.getElementById("date")
  expect(btn).toBeDefined()
})